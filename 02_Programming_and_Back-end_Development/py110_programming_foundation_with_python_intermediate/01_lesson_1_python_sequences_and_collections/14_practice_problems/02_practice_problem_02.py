'''
Consider the set:
numbers = {1, 2, 3, 4, 5, 5, 4, 3}

What is the set's length? Try to answer without running the code.
'''
numbers = {1, 2, 3, 4, 5, 5, 4, 3} # effectively it is {1, 2, 3, 4, 5}
print(len(numbers)) # 5 as 3, 4, and 5 are only counted once each.